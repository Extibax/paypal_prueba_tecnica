@model IEnumerable<PayrollPma.Models.Employee>
@{
    ViewData["Title"] = "Empleados";
}

<div class="d-flex justify-content-between align-items-center mb-3">
    <h2>Empleados <span class="badge bg-secondary">@Model.Count()</span></h2>
    <div>
        @if (Model.Any())
        {
            <a asp-action="ExportPdf" class="btn btn-outline-danger me-2">Exportar PDF</a>
        }
        <a asp-action="Create" class="btn btn-primary">Crear empleado</a>
    </div>
</div>

@if (!Model.Any())
{
    <div class="empty-state">
        <p>No hay empleados registrados.</p>
        <a asp-action="Create" class="btn btn-primary">Crear el primero</a>
    </div>
}
else
{
    <table class="table table-hover table-bordered payroll-table">
        <thead class="table-light">
            <tr>
                <th>Nombre</th>
                <th>Apellido</th>
                <th class="text-end">Rata por hora</th>
                <th class="text-end">Horas mensuales</th>
                <th class="text-center">Acciones</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var emp in Model)
            {
                <tr>
                    <td>@emp.Nombre</td>
                    <td>@emp.Apellido</td>
                    <td class="text-end">@emp.RatePerHour.ToString("N2")</td>
                    <td class="text-end">@emp.MonthlyHours</td>
                    <td class="text-center">
                        <a asp-action="Details" asp-route-id="@emp.Id" class="btn btn-sm btn-info">Ver cálculos</a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
}
